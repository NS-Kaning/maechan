frappe.listview_settings['UserProfile'] = {
    onload(listview) {
        // if (frappe.session.user != "Administrator") {
        //     listview.$page.find(`.page-form .flex`).hide();
        //     frappe.route_options = {
        //         'email': ['=', frappe.session.user]
        //     }
        // }

    }
}